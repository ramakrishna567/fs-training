let promiseToClean = new Promise(function (resolve, reject){
    let isClean = false;
    if(isClean){
        resolve(`The Room is clean`);
    }
    else{
        resolve(`The Room is clean`);        
    }
});