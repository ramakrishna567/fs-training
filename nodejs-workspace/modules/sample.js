module.exports.showMsg = (names, age,msg)=>{
    console.log(`Welcome ${names}
    Your age is ${age} and
    For you a Msg : ${msg}`);
}

module.exports.run = (names, age,msg)=>{
    console.log(`Welcome ${names}
    Your age is ${age} and
    For you a Msg : ${msg}`);
}

var user = {};
user.showData = 